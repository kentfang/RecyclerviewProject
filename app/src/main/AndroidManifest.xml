<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fbw.recyclerviewproject">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- <activity android:name=".popup.PopUpActivity"></activity> -->
        <!-- <activity android:name=".Jzvd.JiaoziPlayActivity"></activity> -->
        <!-- <activity android:name=".manager.LayoutManagerActivity"></activity> -->
        <!-- <activity android:name=".wifi.WifiActivity"></activity> -->
        <!-- <activity android:name="com.fbw.recyclerviewproject.gallery.GralleyVPActivity" /> -->
        <activity
            android:name="com.fbw.recyclerviewproject.lottie.LottieActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".transition.DetailActivity" />
        <activity android:name=".transition.TransitionManagerActivity" />
    </application>

</manifest>